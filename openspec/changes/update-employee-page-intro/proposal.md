## Why

The employee-management page intro currently describes the broader employee and password-management workflow rather than the concise label requested for the page. Updating the copy keeps the screen wording aligned with its intended focus without changing employee behavior.

## What Changes

- Replace the employee-management page intro text with `員工基本資料`.
- Preserve the existing page structure, controls, data grid, routes, API behavior, and styling.

## Capabilities

### New Capabilities

None. This is a presentation-copy adjustment and does not introduce an application capability.

### Modified Capabilities

None. Existing employee-management requirements and observable CRUD behavior remain unchanged.

## Impact

- Affected code: `apps/frontend/src/pages/EmployeesPage.jsx`.
- No API, dependency, database, or migration changes are required.
