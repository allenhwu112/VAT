## 1. Employee page copy

- [x] 1.1 Replace the employee-management intro with `員工基本資料` in `EmployeesPage.jsx`, preserving the existing `.intro` element and surrounding layout; verify the source contains the exact requested text.

## 2. Verification

- [x] 2.1 Run the frontend lint and production build, then refresh `/VAT_UI/employees` to verify the new intro text appears and the page controls and grid remain visible.
