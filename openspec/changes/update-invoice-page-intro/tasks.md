## 1. Invoice page copy

- [x] 1.1 Replace the invoice-management intro with `客戶使用的發票種類` in `InvoicesPage.jsx`, preserving the existing `.intro` element and surrounding layout; verify the source contains the exact requested text.

## 2. Verification

- [x] 2.1 Run the frontend lint and production build, then refresh `/VAT_UI/invoices` to verify the new intro text appears and the page controls and grid remain visible.
