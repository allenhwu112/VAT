## 1. Client page copy

- [x] 1.1 Replace the client-management intro with `客戶基本資料及稅務資訊` in `ClientsPage.jsx`, preserving the existing `.intro` element and surrounding layout; verify the source contains the exact requested text.

## 2. Verification

- [x] 2.1 Run the frontend lint and production build, then refresh `/VAT_UI/clients` to verify the new intro text appears and the page controls and grid remain visible.
